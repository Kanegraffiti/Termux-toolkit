# Contributing

To add a script:
1. Place it in `scripts/<category>/` and include a header comment with `# @tool <name>` and description.
2. Provide a corresponding manual page in `man/<category>/<name>.man`.
3. Run tests in `scripts/test/` before opening a pull request.
