pkg-toolkit - manage package batches

Usage:
  pkg-toolkit --list
  pkg-toolkit --status
  pkg-toolkit --install <batch>
  pkg-toolkit --uninstall <batch>

Package batches are defined in pkg-batches.conf. Use --status to check installed packages.

